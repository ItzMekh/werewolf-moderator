services:
  - type: web
    name: werewolf-server
    runtime: node
    region: singapore
    rootDir: server
    buildCommand: npm install && npx prisma generate
    startCommand: node index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
